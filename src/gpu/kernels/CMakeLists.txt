SET(NAME kernels)
SET(HEADERS
    bgra_2_gray.h
    transpose.h
    match.h
    cudamath.h
    resample.h
    downsample.h
    convolution.h
    ransac.h
    keypoint.h
    orientation.h
    descriptor.h)
CUDA_ADD_LIBRARY(${NAME}
    ${HEADERS}
    bgra_2_gray.cu
    transpose.cu
    match.cu
    cudamath.cu
    resample.cu
    downsample.cu
    convolution.cu
    ransac.cu
    keypoint.cu
    orientation.cu
    descriptor.cu)
INSTALL_INCLUDES(${HEADERS})
INSTALL_BINARIES(${NAME})
